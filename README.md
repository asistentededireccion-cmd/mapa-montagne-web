# mapa-montagne-web

Shell del mapa de expansion MONTAGNE. **No contiene datos**: los pide en runtime a
una Edge Function privada de Supabase (`map-data`), que exige clave.

Generado por `build.py` del proyecto MapaExpansion; no editar a mano.
